{
  "private": true,
  "name": "monorepo",
  "scripts": {
    "build-circuits": "yarn turbo run test --filter=@flax/circuits..."
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "turbo": "^1.5.5"
  }
}
