{
  "private": true,
  "name": "monorepo",
  "scripts": {
    "compile-circuits": "yarn turbo run compile-circuits --filter=@flax/circuits...",
    "compile-and-setup-circuits": "yarn turbo run build --filter=@flax/circuits..."
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "turbo": "^1.5.5"
  }
}
