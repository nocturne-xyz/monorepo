{
  "name": "@nocturne-xyz/contracts",
  "version": "1.0.5-alpha",
  "description": "Nocturne contracts",
  "license": "MIT OR Apache-2.0",
  "author": "Luke Tchang",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": "18.12.1"
  },
  "scripts": {
    "bootstrap": "yarn clean && yarn build",
    "build": "yarn build:deps && yarn compile && hardhat prepare-package && tsc --build && yarn copy-definitions && FOUNDRY_PROFILE=contracts forge build",
    "build:deps": "scripts/install_deps.sh",
    "clean": "hardhat clean && rm -rf .turbo artifacts cache_hardhat dist src foundry_artifacts",
    "compile": "hardhat compile",
    "deploy": "hardhat run ./deployer/index.ts",
    "copy-definitions": "cp src/*.d.ts dist/src",
    "lint": "solhint contracts/*.sol",
    "lint:fix": "solhint --fix contracts/*.sol && yarn prettier --write",
    "prettier:check": "prettier --check ./contracts",
    "prettier:write": "prettier --write './contracts'",
    "prepublish": "yarn bootstrap && yarn build",
    "test:unit": "FOUNDRY_PROFILE=contracts forge test",
    "test:gas-report": "yarn test:unit --gas-report"
  },
  "dependencies": {
    "@openzeppelin/contracts": "4.7.3",
    "@openzeppelin/contracts-upgradeable": "4.7.3"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-etherscan": "^2.1.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@openzeppelin/hardhat-upgrades": "^1.22.1",
    "@typechain/ethers-v5": "7.2.0",
    "@typechain/hardhat": "^2.0.1",
    "@types/node": "^17.0.14",
    "dotenv": "^10.0.0",
    "eslint": "^7.20.0",
    "ethereum-waffle": "^3.2.2",
    "ethers": "^5.4.4",
    "hardhat": "^2.11.2",
    "hardhat-gas-reporter": "^1.0.4",
    "hardhat-packager": "~1.2.1",
    "prettier": "^2.8.0",
    "prettier-plugin-solidity": "^1.0.0-beta.5",
    "solhint": "^3.3.2",
    "solhint-plugin-prettier": "^0.0.5",
    "ts-node": "^10.1.0",
    "typechain": "^5.0.0",
    "typescript": "^4.5.5"
  }
}
