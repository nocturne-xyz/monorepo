type DepositEvent @entity(immutable: true) {
  id: Bytes!
  type: DepositEventType!
  chainId: BigInt!
  spender: String!
  encodedAssetAddr: BigInt!
  encodedAssetId: BigInt!
  value: BigInt!
  depositAddrH1X: BigInt!
  depositAddrH1Y: BigInt!
  depositAddrH2X: BigInt!
  depositAddrH2Y: BigInt!
  nonce: BigInt!
  gasCompensation: BigInt!
}

enum DepositEventType {
  Instantiated
  Retrieved
  Processed
}

type EncodedAsset @entity(immutable: true) {
  id: Bytes!
  encodedAssetAddr: BigInt!
  encodedAssetId: BigInt!
}

type StealthAddress @entity(immutable: true) {
  id: Bytes!
  h1X: BigInt!
  h1Y: BigInt!
  h2X: BigInt!
  h2Y: BigInt!
}

type EncodedOrEncryptedNote @entity(immutable: true) {
  id: Bytes!
  merkleIndex: BigInt!
  note: EncodedNote
  encryptedNote: EncryptedNote
}

type EncodedNote @entity(immutable: true) {
  id: Bytes!
  ownerH1X: BigInt!
  ownerH1Y: BigInt!
  ownerH2X: BigInt!
  ownerH2Y: BigInt!
  nonce: BigInt!
  encodedAssetAddr: BigInt!
  encodedAssetId: BigInt!
  value: BigInt!
}

type EncryptedNote @entity(immutable: true) {
  id: Bytes!
  ownerH1X: BigInt!
  ownerH1Y: BigInt!
  ownerH2X: BigInt!
  ownerH2Y: BigInt!
  encappedKey: BigInt!
  encryptedNonce: BigInt!
  encryptedValue: BigInt!
  encodedAssetAddr: BigInt!
  encodedAssetId: BigInt!
  commitment: BigInt!
}

type Nullifier @entity(immutable: true) {
  id: Bytes!
  nullifier: BigInt!
}

type SubtreeCommit @entity(immutable: true) {
  id: Bytes!
  newRoot: BigInt!
  subtreeIndex: BigInt!
}
