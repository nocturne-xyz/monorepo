import { SubtreeUpdateInputSignals } from "@flax/sdk";
import { Wallet } from "@flax/contracts";

export async function genAndSubmitProof(inputs: SubtreeUpdateInputSignals, contract: Wallet): Promise<void> {
	return;
}
