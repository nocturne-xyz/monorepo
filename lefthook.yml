#   Refer for explanation to following link:
#   https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
pre-commit:
  parallel: true
  commands:
    prettier:
      run: yarn prettier:check
    lint:
      run: yarn lint
    fresh-build:
      run: yarn turbo run --no-daemon build --no-cache --force
